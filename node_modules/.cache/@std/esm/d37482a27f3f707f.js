"main";let test;_7f3‍.w('tape',[["default",function(v){test=v}]]);let glob;_7f3‍.w('glob',[["default",function(v){glob=v}]]);let path;_7f3‍.w('path',[["default",function(v){path=v}]]);let load;_7f3‍.w('load-json-file',[["default",function(v){load=v}]]);let write;_7f3‍.w('write-json-file',[["default",function(v){write=v}]]);let rectangleGrid;_7f3‍.w('.',[["default",function(v){rectangleGrid=v}]]);






test('turf-rectangle-grid', t => {
  glob.sync(path.join(__dirname, 'test', 'in', '*.json')).forEach(filepath => {
    // Define params
    const {name} = path.parse(filepath);
    const geojson = load.sync(filepath);
    // etc.

    const results = [];
    const out = filepath.replace(path.join('test', 'in'), path.join('test', 'out'));
    if (process.env.REGEN) write.sync(out, results);
    t.deepEqual(results, load.sync(out), name);
  });
  t.end();
});
